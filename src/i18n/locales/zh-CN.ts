// 中文（简体）翻译资源。按域组织（common / app / toolbar / sidebar / history /
// workspace / diff / branch / settings / terminal / errors / misc），内容由后续
// commit 分批填充。当前骨架阶段仅存在结构占位。
const messages = {
} as const

export default messages
export type MessageSchema = typeof messages
