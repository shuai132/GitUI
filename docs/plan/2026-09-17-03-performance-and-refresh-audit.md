# 性能与刷新一致性优化

## Context

只读排查确认了八类问题：Diff 的平方级索引查找及全量渲染、历史分页重复 revwalk、丢失引用的重复祖先扫描、同步 Git 工作占用异步线程、linked worktree 监听遗漏、刷新缩减历史分页、引用元数据比较不完整，以及工作区 diff 统计重复计算。

目标是逐项修复并分别提交，保持现有 Git 语义、默认偏好、分页与搜索范围、Diff 模式、键盘导航和文件 / Hunk 操作。已有虚拟滚动、防抖、请求过期保护和确认上下文校验继续生效。

## 进度总览

| 阶段 | 对应排查项 | 状态 |
| --- | --- | --- |
| 1. 引用元数据刷新 | 7 | 已完成 |
| 2. 历史刷新保留分页与浏览锚点 | 6 | 已完成 |
| 3. Diff 行索引与按需高亮 | 1 | 已完成 |
| 4. 工作区统计合并 | 8 | 已完成 |
| 5. 实际 Git 元数据目录监听 | 5 | 已完成 |
| 6. 丢失引用祖先遍历合并 | 3 | 已完成 |
| 7. 历史遍历复用与缓存失效 | 2 | 已完成 |
| 8. Git 阻塞任务调度 | 4 | 已完成 |
| 9. 大数据验证与完成审计 | 全部 | 已完成 |

## 子任务清单

- [x] 比较分支与标签的完整业务字段，验证上游变更、标签重建和无变化时引用复用。
- [x] 同仓库 / 同过滤条件刷新保留已加载范围，验证历史变化、分页竞态与滚动锚点。
- [x] 移除 Diff 逐行线性反查，将大文件的词级比较与高亮限制在接近可见的分块，并验证搜索、换行、滚动同步、选择复制、变更跳转和 Hunk 操作。
- [x] 合并工作区已跟踪与未跟踪文件的 diff 统计，验证各状态和类型变更。
- [x] 监听工作目录、实际 gitdir 和共享元数据，验证普通仓库、linked worktree、submodule 与切仓库释放。
- [x] 共用祖先遍历，验证 reflog tip / reachable / stash 标记不变。
- [x] 复用历史顺序及标记，明确引用、reflog、过滤条件变化和仓库关闭的缓存失效与容量边界。
- [x] 将耗时 Git 任务放入受控阻塞执行器，验证任务排队不会占用异步工作线程，保持命令结果和错误语义。
- [x] 每个主题完成格式化、四项构建 / 测试检查、暂存差异审查并独立提交。
- [x] 使用几千至一万提交及大文本 Diff 做对照验证，记录方法、结果与环境限制。

## 关键决策

- 不以关闭丢失引用、减少搜索范围、隐藏统计或截断正常 Diff 来规避开销；优化内部计算、缓存及渲染。
- 引用比较显式覆盖领域类型字段，仍保留未变化时不赋值的响应式优化。
- 历史缓存以仓库和实际 Git 快照 / 过滤条件隔离，不单靠 watcher 通知判断有效性，避免漏事件后读到陈旧结果。
- 历史缓存只保存排序后的 OID 与渲染标记，提交正文仍按页读取；用实际 gitdir、引用入口、HEAD / stash reflog 入口、过滤条件及浅克隆边界判断有效性。同快照的并发构建合并，缓存按最近使用及总行数有界保留，关闭仓库主动释放。
- 同仓库 / 同过滤条件刷新先检查快照身份；未变时保留全部已加载分页。变化时读取原有范围并一次替换，分页途中变化则有界重试，失败保留旧列表。滚动以可见提交 OID 为锚点，在该提交仍位于已加载范围时保留行内偏移；切仓库、过滤与搜索不复用旧锚点。
- `LogPage` 增加不透明快照标识，供前端保留未变化的分页及拒绝拼接不同快照。缓存淘汰允许标识变化，调用方保守重读；不改变分页、搜索或图过滤的内容语义。
- Reflog tip 判定共用一次祖先访问集合，在已知可达提交处停止；只保留需要标记的 reflog 入口，不为每个入口重复扫描整段公共历史。
- Watcher 在激活时解析工作目录、实际 gitdir 与 commondir，合并被父目录覆盖的监听根；事件过滤与分类共享这组路径。路径别名在激活时解析，删除事件无需逐路径访问磁盘；新增监听继续使用同一个有界合并窗口，切仓库一起释放。
- 后端跨任务只传路径和可发送的数据；Git 仓库句柄在执行任务的线程内打开和使用。Git 命令共用有界阻塞执行入口，本地读写与网络任务分别限流，先异步等待名额再占用阻塞线程，避免慢网络耗尽本地查询容量。已启动的同步操作自然完成，不伪装可取消；等待期间取消不执行操作，执行中的名额在真实结束后释放。自动 fetch 运行中改间隔不重叠启动新批次，排队完成后复核激活仓库。
- Diff 使用稳定的全局行索引，行配对只做线性扫描；词级比较与语法高亮按接近可见区域的分块惰性执行，首次访问后复用结果。文本节点保留，继续使用浏览器原生搜索、选择和复制，不采用会漏掉屏幕外内容的 DOM 卸载。选区相交的分块暂缓替换高亮，防止正在选择或搜索命中的文本 Range 被破坏。
- Diff 优化保留线性的纯文本 DOM 成本，避免引入替代原生选择的编辑器或用估计宽高改变长行滚动、换行对齐；本轮重点消除平方级反查及首屏全量词级计算，以大文件对照验证效果。
- 如需变更 IPC，同步更新 `docs/11-ipc.md`；设计文档只记录契约与取舍，不复制实现。

## 验证方式

每次代码提交前执行 `cargo fmt`、`npx vue-tsc --noEmit`、`npm run test`、`cargo check` 和 `cargo test`。前端当前没有格式化脚本，不新增格式化工具。

端到端路径：打开普通仓库与 linked worktree，浏览多页历史后切换窗口 / 手动刷新；外部修改 upstream、重建同提交标签、创建分支与暂存文件；检查界面同步且浏览位置稳定。打开大文本文件，在 inline / side-by-side、分块 / 完整文件、换行和高亮状态之间切换，搜索屏幕外文本并执行变更跳转、复制和 Hunk 操作。对有大量提交与失联 reflog 条目的临时仓库重复分页，比较扫描量与耗时；并发慢任务时验证其他 IPC 仍可响应。

基线：前端 530 个测试、后端 138 个测试及类型 / 编译检查通过。临时一万提交仓库的调试构建中，每页 200 条累计读取约 27.6 秒，直接引擎一次读取约 0.54 秒；100 个失联提交使首屏读取从约 0.48 秒增加到 2.80 秒。该数据仅作本机后端对照，不代表 WebView 端到端耗时。

## 实施记录

- 阶段 1：分支比较补齐 upstream / 引用类别，标签比较补齐精确对象及附注元数据。新增 9 个回归用例；前端 539 个、后端 138 个测试及类型 / 编译检查通过。
- 实施顺序调整：阶段 6、7 先于阶段 2，避免为保留多页历史而在旧 revwalk 实现上增加重复扫描；阶段编号仍对应上表。
- 阶段 6：祖先展开共用去重集合并在可达边界停止，只保存需要判断 tip 的失联入口。前端 539 个、后端 140 个测试及类型 / 编译检查通过。

- 阶段 7：有界缓存排序 OID 与标记，按实际输入失效并返回快照标识；新增缓存并发、失效、容量和分页一致性回归。前端 539 个、后端 146 个测试及类型 / 编译检查通过。同一万提交调试构建对照：50 页累计约 0.79 秒，缓存命中单页约 11 毫秒；100 个失联提交的冷读约 1.11 秒。仅为本机后端测量，未将其等同于 WebView 渲染性能。

- 阶段 2：同快照保留全部分页，变化时按原范围一致读取；分页竞态有界重试，过期请求不覆盖当前上下文。新增 OID 滚动锚点及 18 个刷新 / 导航回归用例。前端 557 个、后端 146 个测试及类型 / 编译检查通过。

- 阶段 4：未暂存 / 未跟踪条目共享一次工作区 diff 与统计遍历。新增混合状态、空文件 / 二进制 / 递归未跟踪目录、unborn HEAD 和类型变更回归；前端 557 个、后端 149 个测试及类型 / 编译检查通过。

- 阶段 5：真实工作目录 / gitdir / commondir 合并监听，过滤与事件分类共享路径映射。临时 linked worktree 和 submodule 布局的真实 HEAD / 共享 refs 写入均收到通知，切仓库释放全部旧回调；新增路径别名与分类回归。前端 557 个、后端 154 个测试及类型 / 编译检查通过。

- 阶段 8：Git IPC 共用本地 / 网络独立限流的阻塞执行入口，自动 fetch 的运行名额跨定时器重启保留。新增异步线程隔离、限流 / 取消、panic / 错误释放、排队后 HEAD 上下文复核等 5 个测试；所有直接调用 GitEngine 的异步命令均已接入。前端 557 个、后端 159 个测试及类型 / 编译检查通过。


- 阶段 3：分组携带全局索引；行配对只建立索引，首屏 / 邻近分块才计算词级和语法 HTML，当前数据上下文复用结果。新增 8 个回归用例，覆盖跨块配对、完整文本 / 全文件上下文、SFC 语言分派、上下文失效、原生选区与同文件刷新保护、Hunk 操作和观察器释放。

## 最终验证记录

- 全量检查：前端类型检查、565 个前端测试、Rust 编译检查和 159 个后端测试通过；Rust 已统一格式化。前端未配置格式化器，沿用现有风格；本轮未新增用户功能，因此 README 功能清单无需改动。
- 历史：临时一万提交仓库的调试引擎对照，50 页累计约 27.6 秒降至 0.79 秒，缓存命中单页约 11 毫秒；含 100 个失联提交的冷读约 2.80 秒降至 1.11 秒。覆盖分页拼接、引用 / reflog / 浅克隆输入失效、缓存容量、刷新竞态与原有浏览锚点。
- Diff：使用真实组件和主题的 Chrome Headless / Vite 临时夹具，对比本阶段前版本；一万源文件行、100 个 Hunk，交替先后顺序运行三轮。行内首屏中位数约 1.02 秒降至 0.49 秒，左右约 1.23 秒降至 0.55 秒；首屏词级 mark 从 18000 个分别降至 120 / 226 个，完整文本行数不变。
- 交互：一千行含长行 / 中文的浏览器夹具覆盖 inline / side-by-side × 分组 / 全文 × 换行 / 不换行八种组合。屏幕外原生搜索、跨块选区所得文本与旧版对照、顶部行锚点、变更导航、Hunk 按钮、左右横向滚动同步、换行共同网格对齐通过；单元测试补充高亮开关、语言解析与刷新期间选区保护。
- 后端集成：linked worktree 的私有 HEAD / 共享 refs、分离 gitdir 的 submodule 写入均使用真实文件系统通知验证；阻塞队列测试确认慢网络不占用本地队列、取消不会提前释放运行名额，排队期间 HEAD 变化仍由执行时上下文校验拒绝过期写入。
- 测量边界：以上分别为本机调试引擎及 Chrome 组件夹具，未把数据视为打包后的 Tauri WebView 全应用耗时。Diff 保留线性纯文本 DOM 成本，以保持原生搜索和复制；不截断文件、不卸载文本行、不改变默认功能偏好。
