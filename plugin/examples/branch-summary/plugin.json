{
  "api_version": 1,
  "id": "com.gitui.demo.branch-summary",
  "name": "Demo: Branch Summary",
  "version": "0.1.0",
  "description": "Reports the current branch and commit count using local git commands.",
  "backend": {
    "command": "node",
    "args": ["backend.mjs"]
  },
  "permissions": [
    "git:read",
    {
      "id": "process:run",
      "reason": "Runs read-only git commands in the active repository."
    }
  ],
  "contributes": {
    "commands": [
      {
        "id": "branchSummary.show",
        "label": "Show branch summary",
        "category": "Demo",
        "description": "Shows the current branch name and reachable commit count."
      }
    ],
    "menus": [
      {
        "location": "toolbar.actions",
        "command": "branchSummary.show"
      },
      {
        "location": "commit.context",
        "command": "branchSummary.show"
      }
    ]
  }
}
